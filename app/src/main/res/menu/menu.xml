<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/login_status"
        android:icon="@drawable/profile_symbol"
        android:orderInCategory="1"
        android:title="@string/menu_Login_status"
        app:showAsAction="ifRoom">

        <menu>
            <item
                android:id="@+id/login_menu_item"
                android:title="@string/action_login"
                android:visible="true"/>

            <item
                android:id="@+id/logout_menu_item"
                android:title="@string/action_logout"
                android:visible="false"/>
        </menu>

    </item>

    <item
        android:id="@+id/shopping_cart"
        android:icon="@drawable/shopping_cart"
        android:orderInCategory="2"
        android:title="@string/menu_shopping_cart"
        app:actionLayout="@layout/cart"
        app:showAsAction="ifRoom" />

</menu>